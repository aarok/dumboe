import Typography from 'typography'

const typography = new Typography({
    baseFontSize: '18px',
    headerFontFamily: ['Helvetica Neue', 'nimbus-sans', 'helvetica', 'sans'],
    bodyFontFamily: ['Helvetica Neue', 'nimbus-sans', 'helvetica', 'sans'],
    blockMarginBottom: '0'
})

module.exports = typography